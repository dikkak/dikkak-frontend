import React from 'react';
import Menu from '../components/Menu';

const Login = () => {
  return (
    <div>
      <Menu/>
      <div style={{marginTop:'50px'}}>Log In</div>
    </div>
  );
};

export default Login;